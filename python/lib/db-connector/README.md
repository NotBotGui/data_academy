# db-connector

Shared internal library for SQL Server and Snowflake connection helpers.

## Install (editable)

```bash
pip install -e python/lib/db-connector
```

## Usage

```python
from db_connector import ...
```
